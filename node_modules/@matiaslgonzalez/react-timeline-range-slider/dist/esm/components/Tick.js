import React from 'react';
import { getMinutes } from 'date-fns';
import { TickLabel, TickMarker } from './StyledComponents';
var Tick = function (_a) {
    var tick = _a.tick, count = _a.count, format = _a.format;
    var isFullHour = !getMinutes(tick.value);
    var tickLabelStyle = {
        marginLeft: "".concat(-(100 / count) / 2, "%"),
        width: "".concat(100 / count, "%"),
        left: "".concat(tick.percent, "%"),
    };
    return (React.createElement(React.Fragment, null,
        React.createElement(TickMarker, { isFullHour: isFullHour, style: { left: "".concat(tick.percent, "%") } }),
        isFullHour && React.createElement(TickLabel, { style: tickLabelStyle }, format(tick.value))));
};
export default Tick;
//# sourceMappingURL=Tick.js.map