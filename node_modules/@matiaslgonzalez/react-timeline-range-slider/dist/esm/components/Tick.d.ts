import React from 'react';
interface TickProps {
    tick: {
        id: string;
        value: number;
        percent: number;
    };
    count: number;
    format: (value: number) => string;
}
declare const Tick: React.FC<TickProps>;
export default Tick;
